[workspace]
resolver = "2"
members = [
    "core",
    "grpc_server",
    "desktop_fltk",
    "wasm_api"
]

# Оптимизации для всех релизных сборок
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true