apiVersion: v1
kind: Service
metadata:
  name: if97-calculator-service
  labels:
    app.kubernetes.io/name: if97-calculator-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: if97-calculator-service
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
      appProtocol: grpc
