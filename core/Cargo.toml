[package]
name = "if97_core"
version = "1.0.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tracing = "0.1"

[dev-dependencies]
csv = "1.3"
serde = { version = "1.0", features = ["derive"] }
